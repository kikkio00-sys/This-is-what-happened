# 0002 V0.1 no live auth or database

V0.1 validates the shell and commissioning experience. It does not persist user-created private content, implement live authentication, or connect a database.

Architecture must keep clear extension points. Future authorization must be enforced server-side; this prototype does not make fake security claims.
