export function PrivacyStates(){const states=[['Personal','Default. Only you can see it in the prototype unless you intentionally choose another state.'],['Invited','Only explicitly invited people receive access.'],['Published','Intentionally released publicly by you. Nothing becomes public automatically.']];return <div className="grid cols">{states.map(([h,p])=><article className="card" key={h}><h2>{h}</h2><p>{p}</p></article>)}<p className="card">Plain-language prototype note: this does not imply Non Friction Designs can browse private customer content.</p></div>}
